# TODO-0900 — Мульти-школы / multi-tenant

**Приоритет:** P4 (roadmap)
**Статус:** open

## Контекст
Если решение взлетит, его захотят другие танцшколы. Архитектурно нужно подготовить multi-tenant.

## Задача
1. Все таблицы получают `tenant_id`.
2. Subdomain routing: `almalatina.app.example.com`, `salsalibre.app.example.com`.
3. Per-tenant брендинг (логотип, цвета) — загружается с сервера, инжектится в CSS-переменные.
4. Tenant-scoped Telegram-боты (один процесс, несколько токенов) или SaaS-бот, к которому школы подключают свой.
5. Биллинг тарифа за tenant (TODO-0600 reuse).

## Acceptance criteria
- [ ] Две школы работают в одном инстансе без утечек данных.
- [ ] Брендирование переключается per домен.

## Файлы
- глубокий refactor — отдельный design-doc перед стартом
